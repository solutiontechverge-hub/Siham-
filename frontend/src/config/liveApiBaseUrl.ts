/**
 * Default deployed backend origin (no trailing slash).
 * Change this when your live API host changes. Optional override: NEXT_PUBLIC_API_BASE_URL in .env* or Vercel.
 */
export const LIVE_API_BASE_URL =
  "https://siham-x1lo-git-main-techvergesolutions-projects.vercel.app";
