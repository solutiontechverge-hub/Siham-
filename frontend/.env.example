# Optional: overrides default in src/config/liveApiBaseUrl.ts
# NEXT_PUBLIC_API_BASE_URL=https://your-backend.vercel.app

# Local API only — add to .env.local
# NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
