# Copy to .env.local to override. See .env.development and .env.production.

# Deployed backend (default in repo for dev + production builds)
NEXT_PUBLIC_API_BASE_URL=https://siham-x1lo-git-main-techvergesolutions-projects.vercel.app

# Local backend only — use in .env.local when running API on this machine
# NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
